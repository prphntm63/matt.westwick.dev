import React from 'react';

const Contact = (props) => (
    <div 
    style={{
            margin : 0,
            padding : '15px',
            marginLeft : '300px',
            flexGrow : 1,
            height : '100vh',
        }}
    id={props.id}
    >
        <h1>Contact Page</h1>
    </div>
)

export default Contact